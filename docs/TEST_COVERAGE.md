# Test coverage — what proves each rule

Every `SPEC.md` rule and the proof that holds it. "Hook" = `tools/hooks/pre-commit`.
"Inspector" = no mechanical proof; checked by a cold read of the diff. Updated
in the same commit as any change to a proof (CLAUDE.md, procedure step 5).

## SPEC rules

| Rule | Proof | Status |
|---|---|---|
| 1 Engine pure, no I/O | `packages/engine/test/purity.test.js` (2): every engine source file imports only engine files; no code line names a browser, network, file-system or process global (comments stripped). Both checks shown to fail on a planted `node:fs` import and a planted `window` (S1, 2026-09-23); inspector on Tier A for global state | proven |
| 2 Constants exact | `golden-master.test.js` (19), `starter.test.js` (12), `solver.test.js` (4), `units.test.js` (12), `design-warnings.test.js` scenario 1 (the recommended mash ranges equal the owner's Recipe Designer cells F3 and F4: Rv 1.25–2 qt/lb, R 2.5–4 lb/lb; their disagreement is `grist.js`'s FLAG), `water/**` (135) — a wrong constant fails a pinned value; the 400B band's 800-1000 boundary is a recorded deviation, not a spreadsheet constant (rule 2, 2026-09-21) | proven |
| 3 `// FLAG:` never silent-fix | inspector; box names the FLAG | by design |
| 4 Tolerances never loosened | inspector reads test diffs | gap |
| 5 Public surface is `index.js` only | `apps/recipe/test/spec-rules.test.js` scenario "rule 5": no file under `apps/recipe/src` or `scripts` imports a path inside the engine, or the whole engine as one namespace; shown to fail on a planted deep import (S1, 2026-09-23) | proven |
| 6 60 °F reference; mash water as entered | `smoke.test.js` postBoil 14.5, mashRv 1.7931034 at 60 °F; `options.test.js` scenarios 1–4: the 60 °F defaults leave every derived number the engine's for the uncorrected volumes (within 1e-12); a pre-boil, post-boil, or ferment temperature moves only its own volume, to the engine's `correctVolumeToRef`, and mash Rv stays the as-entered value | proven |
| 7 No brewing math in the app | hook grep (formula signatures) + inspector | proven (hook) |
| 8 One canonical state | `smoke.test.js` drives canonical state through `computeRecipe`; `persistence.test.js` asserts the stored document holds canonical field names and units | partial |
| 9 Convert only at the edges | `smoke.test.js` round-trips: volume 16 gal Home/Pro, SG→Plato, cells 570→0.57 T; `percent.test.js` pins fraction↔percent (0.8↔80, 0.147↔14.7, every reference fraction within 1e-12) and reads every component for leftover `* 100` / `/ 100` ; `spec-rules.test.js` scenario "rule 9": Pro hop weight 16 oz ↔ 1 lb and round trips for 2, 1, 0.5, 1.75 oz in both modes; dry-hop rate 1 oz/gal → 31/16 = 1.9375 lb/bbl and the reference 15 oz in 12 gal → 2.421875 lb/bbl, worked by hand from the engine's 31 gal/bbl and 16 oz/lb | proven |
| 10 Only `selectors.js` calls engine compute | `spec-rules.test.js` scenario "rule 10": every engine function imported by an app file other than `selectors.js` is refused, except `correctVolumeToRef` in `reference-volume.js` (rule 11's slot) and the unit conversions in `display.js` (rule 9); constants may be imported anywhere; the engine's own exports decide what is a function; shown to fail on a planted compute import in a component (S1, 2026-09-23) | proven |
| 11 `toReferenceVolume` routing and correction | `options.test.js` scenarios 2–6: each kind corrected at its own temperature through `computeRecipe` (`refVolumesGal` carries the three); direct: 16 gal at 190 °F equals `correctVolumeToRef(16, 190)`, and NaN, 31, 213, Infinity, a missing map, a missing kind → NaN; an uncorrectable temperature blanks the dependent stats and nothing throws; `reference-temperature.test.js` scenarios 1–2: every "at 60 °F" label and the sheet's temperature-note rule read the engine's `REFERENCE_TEMP_F` (no app source file writes a 60 °F reference of its own, comments aside), and a new recipe's and a version-1 recipe's measurement temperatures are it | proven |
| 12 Smoke test passes unchanged | hook runs it on A/B commits; Netlify runs both suites before every build (`apps/recipe/netlify.toml`) | proven |
| 13 Persisted state canonical, one versioned key (4), rows and yeast checked inside, an unreadable saved copy kept aside, version 1 read as 60 °F, versions 1 and 2 read with an empty name, style and notes, versions 1–3 read with an empty strain and a blank fermentation temperature, unreadable → a new recipe (rule 17), NaN round-trips | `apps/recipe/test/persistence.test.js` (7); `options.test.js` scenarios 7–9: the document carries version 4 and the temperatures, a cleared one round-trips as NaN; a version-1 document loads as the same recipe at 60/60/60 with an empty name, style and notes and is saved back as version 4; version 2 loads with the three empty, versions 3 and 4 load, 0 / 5 / `"1"` / `"2"` / `"3"` / `"4"` → defaults; `identity.test.js` scenario 3: name, style and notes round-trip at version 4; `recipe-file.test.js` scenario 1: an exported recipe file is byte-for-byte the stored document, at version 4, and import reads it with the same reader (a version-2 file reads with an empty name, style and notes); `yeast-card.test.js` scenarios 6–7: the document carries version 4 with the strain (accents and capitals kept) and the fermentation temperature, a blank temperature is `null` in the document and NaN on load; a version-1, 2 or 3 document — in storage or as a file — loads with an empty strain and a blank temperature and every stat as before, and is saved back as version 4. Far end 2026-09-23 (yeast card): a version-3 document the live site wrote (lager, high, attenuation 81 %, pre-boil 7.3) loads on the branch build with every figure on the Recipe tab identical to the live site (OG 1.052, FG 1.010, ABV 5.7 %, SRM 4.5, IBU 46, cells 340, starter 100B 2.64 L / 303 g), strain empty and temperature blank, and is rewritten as version 4; the branch's exported file is version 4 and the live site refuses it as newer, asking nothing and leaving its recipe as it was. Far end 2026-09-23: a version-2 document the live site wrote (pre-boil 8) loads on the branch build with every number on the Recipe tab identical to the live site and the three empty, and is rewritten as version 3; a typed name, style and two-line notes survive a reload with no number moved. Far end 2026-09-20 on the built app: edit → reload holds; Pro → reload holds; Reset cancel unchanged, accept → reload defaults. Far end 2026-09-21: the version-1 document `main` wrote (pre-boil 8) loads as pre-boil 8 with every stat as on `main` and is rewritten as version 2; `saved-rows.test.js` scenarios 1–5: a malt, kettle-hop or dry-hop row (first or last) missing any field, holding the wrong kind of value, or not an object, and a yeast missing a field or with a type or character outside the engine's choices, make the document unreadable, while a blank number and every type × character pair are readable; a damaged file is refused with the damaged message, nothing asked, storage untouched; a damaged saved copy starts a new recipe and is kept aside byte for byte under `brew-design.recipe.unreadable`, through the autosave and a second load, one copy, replaced by the next unreadable one (text that is not JSON, a newer version), blocked storage loads and nothing throws; version 1–4 documents with a blank in every row number and unknown extra fields load, blanks as blanks. Far end 2026-09-23 (saved rows): a hand-damaged file (first malt's name removed) is refused with the damaged message, nothing asked, the recipe on screen and its saved copy unchanged, nothing kept aside; the same document in storage reloads as a new recipe with the damaged text kept aside as found, still there after later loads; a version-4 recipe the live site saved with six blank boxes loads with both tabs, every box, the sheet and the saved document identical to the live site | proven |
| 14 No hex outside `styles.js` | hook grep | proven (hook) |
| 15 Read-only values are plain text | none; visual | by design (look at it) |
| 16 The ingredient list is the owner's workbook, generated, unrounded, never recipe state; a pick copies its numbers | `apps/recipe/test/ingredients.test.js` scenarios 1–4: the copy equals the list read from the workbook field by field with exact equality, in the workbook's order (42 malts, 43 hops, 32 yeasts; Bravo 0.144 as stored; Rice Hulls 0 and 0 °L; accents kept); a row added, removed or renamed, or a number changed, in the workbook alone is named by sheet and row; an unusable workbook (duplicate name ignoring capitals, blank or non-number required number, percentage outside 0–100% incl. 82 typed without %, negative colour, yeast neither Ale nor Lager, lab range inverted or with one end) is refused naming sheet and row, and the refresh writes nothing; two refreshes are byte-identical and equal the committed copy. Hooks: `pre-commit` runs the suite when the workbook, the copy, the refresh tool or the test is staged; `commit-msg` requires a PASS box for the refresh tool or the test. Far end 2026-09-23: nothing imports the copy — the built app is byte-identical to `main`'s (bundle `index-CQcNJ7jR.js`, the live site's); a one-cell edit in a scratch workbook refreshes to exactly one changed line and the test fails naming Hops row 5 (Bravo). Never in recipe state or a saved recipe: the only importer of the copy is the ingredient search, which reads it for suggestions and picks (inspector). A pick copies the numbers, never a reference: `ingredient-search.test.js` scenarios 5 and 7 — a pick equals the same recipe with the list's numbers typed in, field for field and through `computeRecipe`, and a picked row is unchanged when the list object it read from is altered. Far end 2026-09-23 on the built app: a pick saves exactly the workbook's numbers (American Crystal 40 → 0.77 and 40; Bravo → 0.144), identical to typing them | proven |
| 17 The brewery's figures are their own document (version 1), fill only a new recipe, blank → built-in, never change a recipe | `apps/recipe/test/brewery-defaults.test.js` scenarios 1–8 (below). Far end 2026-09-23 on the built app (see the scenario row) | proven |

## Scenarios

| Scenario | File | Count |
|---|---|---|
| grist golden master · hops golden master · yeast golden master · starter golden master (cellsNeeded 570) | `packages/engine/test/golden-master.test.js` | 19 |
| solveStarter band selection (900B/950B/1000B/1010B the 400B band's 800-1000 rule; every count 250-1700 has an option) · intentional boundary gaps (strict inequalities, transcribed as-is) | `packages/engine/test/starter.test.js` | 12 |
| solveGrist round-trip (pinned residual, see solver.js FLAG) | `packages/engine/test/solver.test.js` | 4 |
| design warnings, mash ranges: the mash ranges are the owner's cells: Rv 1.25–2 qt/lb, R 2.5–4 lb/lb · Mash Rv warns outside 1.25–2 and not at either end (hand-worked: 5 gal on 16 lb = 1.25 and on 10 lb = 2.0 inside; 5.5 on 10 = 2.2 and 3 on 10 = 1.2 warn) · Mash R warns outside 2.5–4 and not at either end, each range on its own (4.95 gal on 10 lb: Rv 1.98 inside, R 4.0689 warns; 3.075 gal on 10 lb: Rv 1.23 warns, R 2.52765 inside; 2.5 and 4 inside; a blank gives none) | `packages/engine/test/design-warnings.test.js` | 3 |
| grain-bill share per malt: each malt's share is its weight over the bill total, for the reference recipe (10/11 and 1/11, worked by hand) · the shares sum to 1 for a bill with a positive total weight · a zero-weight malt is listed with a share of 0 · an empty bill and a cleared weight yield shares that are not numbers, and nothing throws | `packages/engine/test/grist.test.js` | 4 |
| basic conversions · water density table · correctVolumeToRef · the engine states its volume reference temperature, 60 °F, and the volume correction defaults to it | `packages/engine/test/units.test.js` | 12 |
| engine purity (rule 1): every engine source file imports only engine files · no engine code line names a browser, network, file-system or process global | `packages/engine/test/purity.test.js` | 2 |
| unit constants · volumeToGallons · volumeUnit · acidMaltUnits | `packages/engine/test/water/units.test.js` | 21 |
| saltContribution · first-principles hand-calculations | `packages/engine/test/water/salts.test.js` | 20 |
| acidCapacity · acidAlkalinityReduction · acidContribution · applyAcids — multi-acid linearity | `packages/engine/test/water/acids.test.js` | 32 |
| residualAlkalinity · sulfateChlorideRatio · ratioCharacter — Palmer-Kaminski Table 5.2 · first-principles (Kolbach) | `packages/engine/test/water/ra.test.js` | 21 |
| solveAdditions — smoke, return shape, alkalinity branch, ion targeting, traced hand-calculations · predictFinalProfile | `packages/engine/test/water/solver.test.js` | 23 |
| reference batches parity | `packages/engine/test/water/reference-batches/parity.test.js` | 18 |
| parity through the UI selector (computeRecipe) · unit-conversion boundary round-trips | `apps/recipe/test/smoke.test.js` | 13 |
| persistence: saved recipe restored (canonical, one versioned key) · cleared field round-trips as NaN · display settings restored · reset removes the saved copy · nothing saved → defaults · unreadable data → defaults · unavailable storage is a no-op | `apps/recipe/test/persistence.test.js` | 7 |
| percent: a fraction shows as a percent · a percent entered parses to the fraction · every reference-recipe fraction round-trips within 1e-12 · ABV 0.0748883 shows as 7.49% · no fraction↔percent arithmetic remains in components | `apps/recipe/test/percent.test.js` | 5 |
| options page: the default temperatures are 60 °F and leave every derived number the engine's for the uncorrected volumes, within 1e-12 · a pre-boil temperature corrects only the pre-boil volume · a post-boil temperature corrects the post-boil volume · a fermentation temperature corrects the fermentation volume · `toReferenceVolume` corrects at the temperature of its kind and returns NaN for one the engine cannot correct · an uncorrectable temperature blanks the dependent stats and nothing throws · the saved document carries version 4 and the temperatures; a cleared one round-trips as NaN · a version-1 document loads as the same recipe with the reference temperatures and an empty name, style and notes, and is saved back as version 4 · a version-2 document loads with an empty name, style and notes; version-3 and version-4 documents load; any other version yields the defaults | `apps/recipe/test/options.test.js` | 9 |
| recipe identity: a new recipe has an empty name, style and notes · the three fields reach no calculation: every derived number is identical with them empty and filled · name, style and notes round-trip through save and load at version 4 | `apps/recipe/test/identity.test.js` | 3 |
| recipe file: export produces the same document the app saves, at the current version · the file name is the recipe name plus today's date; an unnamed recipe uses the fallback · importing a valid file replaces the recipe and the display settings, and becomes the working copy · importing a file that is not a Brew Design recipe is refused and leaves the current recipe untouched · importing a damaged file is refused and leaves the current recipe untouched · importing a file with a newer version is refused, naming that reason · exporting changes no recipe value and no derived number | `apps/recipe/test/recipe-file.test.js` | 7 |
| print sheet: the sheet renders every recipe value from the derived values it is given, computing nothing (the reference recipe against `computeRecipe` through the display boundary, hand-checked shares 93.1/6.9 % and mash Rv 1.79; altered derived values print as given; a blank prints a dash) · an empty notes box produces no notes section · a recipe with no name prints no title, never an invented one · a volume measured at the reference temperature prints no temperature note; one measured elsewhere does · the sheet's numbers follow the display mode: Home and Pro produce different volume and hop-weight text for the same recipe. Far end 2026-09-23: print preview (headless Chrome, Letter) shows the sheet alone; hidden on screen; Home/Pro changes the printed units | `apps/recipe/test/print-sheet.test.js` | 5 |
| searchable malt and hop boxes: typing part of a name finds it: capitals and accents ignored, matched anywhere, names that start with the text first, then the workbook's order · each suggestion shows the numbers a pick would fill, as the boxes show them · picking a malt fills its FGDB and colour and leaves the weight · picking a boil hop fills its alpha and leaves time, temperature and weight; picking a dry hop sets its name only · a pick puts the list's numbers into the recipe exactly, and every stat equals the recipe with those numbers typed · editing the name, even to one on the list, leaves every number as it was · the recipe keeps its own copy: a picked row does not change when the list does · a new row starts with an empty name and today's starting numbers. Far end 2026-09-23 on the built app: "crys" lists the six Crystal malts with FGDB and colour; a pick (click, or arrows + Enter) fills 77 % and 40 °L and leaves the weight, with the same saved row, OG, SRM and points as typing them; a changed FGDB survives a reload; "My Malt" and a hand-typed "American Crystal 40" change no number; Bravo fills 14.4 % and moves IBU 47 → 55 with time, temperature and weight unchanged; a dry-hop pick sets the name only; Pro mode still reads percent; Escape closes the list and fills nothing; at 375 px the list opens past the table's sideways-scroll edge, fully on top, and a tap picks the tapped row; a recipe saved by the live site loads with the same saved document, every box and stat, and a byte-identical printed sheet | `apps/recipe/test/ingredient-search.test.js` | 8 |
| footer: the footer names Persyn Chemical Engineering and Consulting beside the logo, and nothing else · screen readers skip the footer logo (empty text alternative, hidden from assistive technology) · the white-background logo file is gone and nothing under `apps/recipe/src` refers to it. Far end 2026-09-23: the footer below the last section on Recipe and Options, see-through on the tint, water-app spacing, no sideways scroll at 375 px; print preview (headless Chrome, Letter) shows the sheet alone with the see-through logo | `apps/recipe/test/footer.test.js` | 3 |
| Netlify skips unchanged builds: a push that changes only documents, the workbook, tooling or tests does not build · a push that changes the app, the engine or the package files builds · when Netlify cannot tell what changed, it builds (no previous build, the same commit, an unknown commit) · Netlify runs the skip rule before every build (`netlify.toml` names the command; run as Netlify runs it, it builds when it cannot tell). Far end 2026-09-23, the command against real `main` history: 62f385b..71b4f77, 2de02d9..71b4f77 and 44c62ac..2df6e0e (documents and the workbook only) skip; 2df6e0e..62f385b, 99d9d1c..254e706 and 71b4f77..searchable-malt-hop-boxes build | `apps/recipe/test/netlify-skip.test.js` | 4 |
| yeast card: picking a strain names it and sets ale or lager, and changes no other number · the strain search finds a strain by name, lab or product code · a strain on the list shows its lab, product code, ale/lager, lab range and attenuation as information; one not on the list shows none · a fermentation temperature outside the strain's lab range warns; inside, at either end, blank, or with a strain not on the list, it does not · moving the yeast changes no stat: the same attenuation and ale/lager give the same FG, ABV, pitch rate, cells and starter · the saved document carries version 4 with the strain and the fermentation temperature; a blank temperature round-trips as blank · a version 1, 2 or 3 document loads with an empty strain and a blank fermentation temperature, every number as before, and is saved back as version 4 · the printed sheet shows the strain, ale/lager, attenuation and fermentation temperature; a blank prints a dash · a new recipe has an empty strain, a blank fermentation temperature and attenuation 77 %. Far end 2026-09-23 on the built app: the Yeast card sits between Grist and Hops, attenuation on it and gone from Grist, the old card reads "Pitch & Starter"; "us-05" suggests SafAle US-05 (Fermentis, Ale), a pick shows "Fermentis · US-05 · Ale · lab range 64–79 °F · attenuation 80 %", sets Ale, attenuation 81 and the blank temperature unchanged; SafLager W-34/70 (arrows + Enter) sets Lager, pitch rate 1.25 and cells 340 as the live site gives for lager at high character, FG and ABV unchanged; 50 °F warns "Outside this strain's lab range, 54–64 °F", 54 and blank do not; "House strain" reads "Not on your list", no warning at 30 or 90 °F, and typing it left Lager in place; the print sheet's yeast lines read SafLager W-34/70 · Lager · 81.0 · 52; no sideways scroll at 375 px; no console errors | `apps/recipe/test/yeast-card.test.js` | 9 |
| design warnings: the default recipe shows no warning: Rv 20/11 ≈ 1.818, R ≈ 3.736, post-boil 5.5 equals fermentation 5.5 · a fermentation volume above the post-boil volume warns; equal or below does not (5.6 warns; 5.5 and 5.0 do not) · the volume check compares both volumes at the 60 °F reference (5.52 gal measured at 100 °F is 5.4870 gal at 60 °F, below 5.5: no warning; at 60 °F it warns) · a warning changes no number, and a blank value gives none (with all three warning, every number equals the engine's called directly; a cleared mash water, fermentation volume or post-boil temperature gives none and nothing throws). Far end 2026-09-23 on the built app: the default recipe shows none; mash water 5.5 (Rv 2, R 4.11) shows "Should be 2.5–4 lb/lb" alone; mash water 3 (Rv 1.091, R 2.242) shows both, each under its value; fermentation 6 shows "Less wort after the boil than the fermenter volume" under the fermentation volume and 5.5 clears it; Pro mode the same in bbl (0.2 warns, 0.17 does not; mash water 0.1 bbl warns on both); the printed sheet carries no warning while two show on screen; no console errors | `apps/recipe/test/design-warnings.test.js` | 4 |
| ingredient list: the app's ingredient list is exactly the workbook's: every row, every number, in the workbook's order · a row added, removed or renamed, or a number changed, in the workbook but not the copy fails the match and names the row · an unusable workbook is refused, naming the sheet and row: duplicate name, blank or non-number required number, percentage outside 0–100%, negative colour, yeast neither Ale nor Lager, lab range inverted · refreshing twice gives an identical copy | `apps/recipe/test/ingredients.test.js` | 4 |
| brewery defaults: with no brewery figures, a new recipe is today's built-in recipe (Home, °P; also a first visit, and blocked storage) · brewery figures fill a new recipe's batch volume, pre-boil volume, boil-off rate, boil time, measurement temperatures and efficiency, and its Home/Pro and gravity unit; every other field is the built-in one · a brewery figure left blank (null, or NaN) gives the built-in figure · "use this recipe's figures" takes exactly those figures from the recipe on screen, and nothing else; a cleared field is taken as a blank · the brewery figures round-trip through their own saved document at version 1; unreadable, another version, a damaged figure or blocked storage gives the built-in figures and nothing throws · saving brewery figures changes neither the saved recipe nor its document, and a recipe loads as it was saved whatever the brewery figures (a version-1 recipe loads at 60 °F with brewery temperatures of 150/180/68; a recipe file brings its own figures) · forgetting the brewery figures returns a new recipe to the built-in one · a recipe from brewery figures gives the same stats as the same recipe typed by hand. Far end 2026-09-23 on the built app: My brewery below the measurement temperatures; a recipe at pre-boil 16, boil-off 1.5, fermentation 12, efficiency 93, Pro, °P → "Use this recipe's figures" shows 0.516129 / 0.048387 / 0.387097 bbl, 93 %, Pro, °P, and they survive a reload; Reset (confirm "…to your brewery's figures?") from a Home recipe at pre-boil 9, mash water 7 gives Pro, °P, those figures, the built-in malts, hops, yeast and mash water 5, stats 6.51 °P / 1.52 °P / 2.6 % / 2.4 / 21 / 0.22 T as for the same recipe typed; changing three brewery figures moves no stat, the saved copy and the exported file are byte-identical, and a reload shows the same stats; a cleared brewery pre-boil resets to 7 gal; Forget (confirm) blanks the section and Reset gives the built-in recipe (1.055 / 1.013 / 5.7 % / 4.7 / 47 / 210) with the plain "…to defaults?" confirm; a version-4 recipe saved by the live site (Pro, SG, temperatures 150/180/68, brewery figures set to other values) loads with every box, every figure on both tabs and the stats bar identical to the live site, saved back byte-identical; with brewery figures and nothing saved, a first visit starts from them; a version-1 recipe loads at 60/60/60, not the brewery's 100, and is saved back as version 4; no sideways scroll at 375 px; no console errors | `apps/recipe/test/brewery-defaults.test.js` | 8 |
| SPEC rules the suite did not yet check (S1, 2026-09-23): the app imports the engine only through its root (rule 5) · only `selectors.js` calls engine compute functions, `reference-volume.js` its correction and `display.js` its unit conversions (rule 10) · Pro hop weight and dry-hop rate convert by the engine's unit constants, pinned by hand (rule 9) | `apps/recipe/test/spec-rules.test.js` | 3 |
| the 60 °F reference held in one place: every "at 60 °F" label and the sheet's temperature-note rule read the engine's reference (the printed post-boil label carries it; a volume measured at it prints no note, one at 150 °F does, for each kind; no app source file writes a 60 °F reference of its own) · a new recipe's measurement temperatures, and a version-1 recipe's, are the engine's reference · every derived number is unchanged (the guard: the smoke test's pinned values at a new recipe's temperatures). Far end 2026-09-23 on the built app: "Post-boil volume at 60 °F", the Options note and its three "at 60 °F" rows, the printed "Post-boil volume at 60 °F" with no note at 60 and "measured at 150 °F" at 150; a new recipe 60/60/60; a version-4 recipe the live site saved (Pro, SG, 150/180/68) loads with both tabs, every box, the stats bar, the printed sheet and the saved document identical to the live site | `apps/recipe/test/reference-temperature.test.js` | 3 |
| saved rows checked inside: a malt, kettle-hop or dry-hop row missing a field, or holding the wrong kind of value, makes the recipe unreadable (every field of each row kind, missing and of the wrong kind; a row that is text, blank, a list or a number; the last row as well as the first; an undamaged document and a blank number stay readable) · a yeast missing a field, or with a type or character outside its choices, makes the recipe unreadable (every ale/lager × high/moderate/low pair readable) · a damaged recipe file is refused with the damaged message and changes nothing on screen · a damaged saved copy starts a new recipe, and the damaged copy is kept aside, as it was (through the autosave and a second load; one copy; text that is not JSON and a newer version kept as found; blocked storage loads, nothing throws) · every earlier version's saved recipe still loads, blanks as blanks, and unknown extra fields are ignored (the guard; versions 1–4, from storage and as a file). Far end 2026-09-23: see rule 13 | `apps/recipe/test/saved-rows.test.js` | 5 |
| post-boil volume as measured: the recipe's figures include the post-boil volume at its measurement temperature: the pre-boil volume as measured less the boil-off (hand pin 16 − 1.5 × 60/60 = 14.5 gal at 60, 100, 180, 212 °F, blank and 250 °F, and with the pre-boil measured hot; its 60 °F figure is the engine's correction of 14.5 gal) · at a post-boil temperature other than 60 °F the printed post-boil row reads the measured figure at that temperature and the 60 °F figure ("Post-boil volume", "measured at 180 °F", "14.50 (x at 60 °F)"; Pro 14.5/31 = 0.468 bbl; the measured box kept) · at 60 °F the printed row is today's, one figure (and a blank or out-of-range temperature: today's single row, its 60 °F figure blank) · every other derived number is unchanged (the guard: at 150/180/68 °F every other figure equals the engine called directly with the corrected volumes). `design-warnings.test.js` scenario 4's expected figures gained the two new ones. Far end 2026-09-23 on the built app: post-boil at 180 °F, the Volumes card shows "Post-boil volume at 180 °F" 14.5 gal above "at 60 °F" 14.085 gal (Pro 0.468 / 0.454 bbl) and the sheet reads "Post-boil volume, measured at 180 °F: 14.50 (14.08 at 60 °F)"; with those two differences removed, both tabs, every box, the sheet and the saved document hash identical to the live site's for the same saved recipe; at 60 °F everything is identical to the live site | `apps/recipe/test/post-boil-measured.test.js` | 4 |
| phone width (no scenario — the suite has no screen; `docs/items/phone-width.md` D5): on a phone the page never scrolls sideways and nothing is cut off at a card's edge · Volumes, Yeast and Pitch & Starter read in one column, labels on one line, warnings across the card · the stats bar is two rows of three, the starter note under the cells figure · every malt, kettle-hop and dry-hop box shows without a sideways swipe · the header is the brand and Home/Pro, then four equal action buttons · the suggestion lists open fully on screen and a tap picks the tapped row · desktop and the printed sheet unchanged, no number changed. Far end 2026-09-23 on the built app, phone emulation: at 360, 375, 414, 430 and 699 px, Home and Pro, Recipe and Options, the page is exactly the screen's width (the live site: 386 px at 360), no box past a card's edge (live: 49 at 360), no sideways-scrolling box; the stats bar 108 px (live: 166 px at 360 in Pro), pinned while scrolling; every box value and every figure on the page (49 boxes, 35 figures, starter options showing) identical to the unchanged build at 360 px, Home and Pro; the design warnings (mash water 3, fermentation 6) and the Yeast card's (SafLager W-34/70 at 75 °F) span the card; a malt, a kettle hop and a dry hop added, picked, edited and removed with points, IBU and dry-hop rate following; the malt list opens below its box, the hop list above its box near the screen's foot, the strain list below, each fully on screen, and a tap on the second or third row picks that row. Desktop: full-page screenshots of the live site and the branch at 700, 1024, 1280 and 1440 px (Home; Pro at 700, 1024, 1440) and print-media screenshots at 360 and 1280 px are byte-identical; element by element (position, size, style, text) identical at 700, 1024 and 1440 px on both tabs in both modes | far end only | 0 |

Total: engine 191, app 104.

## Gaps and the cost of closing each

| Gap | Cost | Tier when closed |
|---|---|---|
| Component input→state round trip at the UI (rules 8/9) | medium: jsdom + testing-library | D |

## Re-tests owed

None. Netlify deploys `main` at https://brew-design.netlify.app (live since
2026-09-20; 277ad62 the first live commit). Percent helpers re-tested there
2026-09-21: Netlify's bundle is `index-DgrrMrtC.js`, the same content hash
as the builder's local build; FGDB 80/80, efficiency 75, attenuation 77,
alpha 12/6, ABV 5.7%; alpha 14.7 → stored `alphaAcidFraction` 0.147, 56 IBU.
Netlify config re-tested there 2026-09-21 (`main@965d3eb`): the deploy log
shows `npm test` running both suites (engine 179/179, app 34/34) and
passing before `vite build`; the deployed bundle `index-D6fkUgun.js` carries
the same content hash as the local build; the live site loaded and computed
a recipe normally straight after. Rule 12's gap is closed (see the rule
table above); see `docs/items/netlify-config.md` for the full log excerpt.
